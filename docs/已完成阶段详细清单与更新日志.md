# 已完成阶段详细清单与更新日志

> 版本：v4.1
> 最后更新：2026-05-16
> 说明：本文档记录 v0.1.0 ~ v4.1 已完成版本的详细功能清单和各版本更新日志。
> 来源：合并自 `docs/README.md` 的已完成阶段清单 + `docs/技术方案设计.md` 第 13 节开发阶段技术重点。
> 版本总览及待开发计划请参见 `docs/开发路线.md`。

---

## ✅ 已完成阶段详细清单（v0.1.0 ~ v4.1）

### v0.1.0 — 项目初始化 + 基础设施 + 认证模块 ✅
**后端：**
- NestJS 项目骨架搭建
- MySQL/Redis/MinIO 连接配置
- JWT 认证体系 + RBAC 权限模型

**前端：**
- Vue 3 + TypeScript + Vite 项目搭建（路由/Pinia/Element Plus/Axios）
- 登录/注册页面 + 三端布局（管理端/教师端/制作人端）

### v2.1 — 课程基础模型 + 后端核心 CRUD ✅
- Course/Chapter/Lesson Entity 建模
- 课程/章节/课时 CRUD API
- 章节/课时排序功能
- 更新种子数据脚本（6 个分类 + 3 门示例课程 + 章节+课时）
- 更新数据库重置脚本

### v2.2 — UI 设计系统 ✅
- 双主题 CSS 变量体系改造（亮色/暗色 `--va-*` 变量）
- 全局排版/间距/阴影令牌
- 创建 Pinia theme store + ThemeToggle 组件
- 对齐所有布局与页面组件

### v2.3 — 文件存储 + 视频/附件上传 ✅
- MinIO Storage 模块封装（上传/删除/获取URL/判断存在）
- Video entity + Upload/Play/Delete API
- Attachment entity + Upload/Download/Delete API

### v2.4 — 教师端课程创建/编辑完善 ✅
**后端变更：**
- Course entity 增加 `reviewComment`, `reviewerId`, `reviewedAt`, `pendingEdit`, `isFeatured`, `trailerVideoId` 字段
- Lesson entity 增加 `previewDuration` 字段；移除 `trailerUrl`
- 新增封面上传接口、课程类型不可修改校验
- 已上架课程编辑后 `pendingEdit` 标记

**前端变更：**
- 拆分创建路由：单课 `/create-single`、系列课 `/create-series`
- CourseEdit.vue 多 Tab 编辑模式（基本信息/视频/试看/资料/章节）
- 视频上传集成（进度条 + 视频库选择）、封面上传
- 课程列表完成度指标 + 状态筛选 + 驳回原因展示

### v2.5 — 撤回审核 + 视频预览 + 自动填充时长 ✅
**后端变更：**
- 新增 `DELETE /courses/:id/withdraw-review` 撤回审核接口
- 视频上传后解析时长并自动填充到课时

**前端变更：**
- 撤回审核功能（待审核→草稿，编辑/下架申请→恢复已上架）
- 课程编辑页视频预览弹窗（HTML5 video 播放器）

### v2.6 — 教师端附件管理前端 ✅
**后端变更：**
- 附件软删除改为真删除 + MinIO 清理；课件独立审核字段预留

**前端变更：**
- AttachmentManager.vue 组件（拖拽上传/进度条/下载/删除/审核状态展示）
- 集成到 CourseEdit.vue「课程资料」Tab

### v2.7 — 管理端审核工作台 + 制作人端课程浏览 ✅
**后端变更：**
- 审核模块 `course_reviews` 表 + CRUD
- `GET /courses/pending-review`、`POST /courses/:id/review`
- `GET /courses/admin`、客户端 `GET /courses` 及详情、试看地址

**前端变更：**
- 管理端审核工作台 `CourseReview.vue`、课程列表 `AdminCourseList.vue`
- 首页 `HomePage.vue`（Hero 视频 + 热门课程 + 关于我们）
- 课程中心 `CourseCatalog.vue`、公开详情 `CourseDetailPublic.vue`
- 全站术语统一："学员/学生" → "制作人"，路由前缀 `/student` → `/producer`
- 未登录试看功能（HTML5 `<video>` + previewDuration 限制）
- 审核员侧边栏精简 + 控制台待办适配

### v2.8 — 教师运营中心（仪表盘 + 制作人管理 + 收益 + 设置） ✅
**后端变更：**
- 教师收益表 + 收益概览/明细/提现 API
- Teacher entity 增加 paymentAccount/notificationEnabled
- AdminModule 统计 + 用户管理 + 教师管理增强

**前端变更：**
- `TeacherDashboard.vue`（数据卡片 + 收益趋势图）
- `TeacherProducers.vue`（已购课制作人列表）
- `TeacherEarnings.vue`（收益明细 + 提现申请）
- `TeacherSettings.vue`（v3.1 迁移到个人中心）

### v2.9 — 管理端增强 ✅
**后端：** AdminModule 统计接口 + 用户管理（搜索/筛选/启用禁用/删除）+ 教师管理增强
**前端：** AdminDashboard 真实数据绑定 + 创建审核员/运营员账号弹窗

### v3.1 — 订单模块 + 购物车 + 个人中心 ✅
**后端变更：**
- Order/OrderItem/CartItem entity + 订单状态机（pending/paid/refunded/cancelled）
- 购物车 CRUD、订单 CRUD、管理端统计
- UserCourse 关联完善 + 退款处理

**前端变更：**
- `Cart.vue`（购物车）、`Checkout.vue`（结算页）
- `MyOrders.vue`（我的订单）、`OrderManage.vue`（管理端订单管理）
- `ProfilePage.vue`（三端统一个人中心）
- 通知偏好独立即时保存、教师设置迁移到个人中心
- 修复跳转 404 + 超管统计数据丢失

### v3.2 — 支付宝支付对接 + 退款处理 ✅
**后端变更：**
- Alipay SDK 集成（沙箱）、createPayment/confirmPayment/processNotify/refund
- 支付成功 → UserCourse/UserLesson 创建 + 学员数更新（事务）
- 退款 → 权限收回 + 学员数扣减（事务）
- 幂等处理、OpenSSL 3.x 兼容修复

**前端变更：**
- `payment.ts` API 层、`PaymentSuccess.vue` 支付成功页
- Checkout/MyOrders/OrderManage 支付状态联调

### v3.3 — 系列课课时单独购买 + 优惠券模块 ✅
**课时单独购买：**
- Lesson 新增 `canSinglePurchase`/`singlePrice` 字段
- UserLesson 实体、OrderItem 扩展 `lessonId` + SINGLE_LESSON 类型
- 播放权限检测（已购课时/已购课程/免费/试看）

**优惠券模块：**
- Coupon/UserCoupon entity、DTO、CRUD、领券/折扣计算/统计
- 管理端 `CouponManage.vue`（创建/编辑/启禁用/删除）
- 用户端可用券列表 + 下单计算折扣

### v3.4 — 收益模块补全 ✅
**后端变更：**
- Earning entity（教师ID/订单ID/课程ID/金额/分成/类型/状态）
- recordEarning（支付成功自动创建）/ refundEarning（退款扣减）
- 教师统计（总收入/可提现/待结算/已提现）
- 平台统计（总流水/分成/趋势/排行榜 TOP10）
- Teacher entity 新增 `totalEarnings`/`withdrawableBalance`
- seed-earnings-demo.js 演示数据（30天96笔订单/24405元流水）

**前端变更：**
- `AdminEarnings.vue` 超管收益看板（趋势柱状图 + 课程/教师排行榜）
- `TeacherEarnings.vue` 教师收益页（后端实际可用）
- earnings.ts API 层 + 路由/侧边栏导航

### v4.1 — 视频播放器组件 + 学习进度追踪 ✅
**目标：** 开发专用视频播放器组件，实现学习进度自动记录和断点续播。

**后端变更：**
- 新建 LearningModule（learning_progress 表 + Entity/Service/Controller/DTO/Module）
- 4 个 API 端点：更新进度/断点续播/课程进度/课时进度列表
- progress >= 95% 自动标记为 completed，仅当 lastPosition 更大时更新

**前端变更：**
- `VideoPlayer.vue` 专用播放器组件（1008 行），支持速度调节/画中画/全屏/键盘快捷键/试看控制
- `CoursePlayer.vue` 重构（745 行），集成 VideoPlayer + 学习进度同步 + 断点续播 + 课件下载
- `frontend/src/api/learning.ts` 新建 API 层

---

## 📝 更新日志

### 版本变更记录（完整详细版，按时间从新到旧排列）

| 日期 | 版本 | 变更内容 |
|------|------|---------|
| 2026-04-30 | v0.1.0 | Phase 1 完成 — 项目骨架搭建完成，认证与基础模块可用 |
| 2026-04-30 | - | 将 Phase 2 拆分为多个迭代小版本以提高交付效率 |
| 2026-05-01 | v2.1 | 课程基础模型 + 后端核心 CRUD 完成，含测试指南、自动化测试脚本、6 个问题修复 |
| 2026-05-06 | v2.2 | UI 设计系统改造 — Vuestic UI 双主题 CSS 变量体系完成，所有页面适配亮色/暗色主题 |
| 2026-05-08 | v2.3 | 文件存储模块（MinIO）+ 视频/附件上传 API 完成，含测试指南、自动化测试脚本 |
| 2026-05-09 | v2.4 | 教师端课程创建/编辑完善（核心流程闭环）— 单课/系列课创建分离、多 Tab 编辑、审核流程后端、试看模型修正 |
| 2026-05-10 | v2.5 | 撤回审核申请 + 视频预览 + 自动填充视频时长 |
| 2026-05-10 | v2.6 | 教师端附件管理前端完成（AttachmentManager 组件 + CourseEdit 集成 + 课件独立审核 + 硬删除重构） |
| 2026-05-10 | - | **文档同步**：对齐 4 个文档的版本号与完成状态；Phase 2-p7/p8 更名为 v2.7/v2.8；Phase 3/4/5 拆分为 p1/p2/p3 子版本（v3.1~v5.2） |
| 2026-05-11 | v2.7 | **审核员权限精简**：侧边栏根据角色动态显示（审核员仅控制台/课程审核/附件审核）；控制台适配审核员身份展示审核待办统计（待审核课程数 + 待审核附件数）；后端新增统计接口 |
| 2026-05-12 | v2.7 | **v2.7 完成确认** — 管理端课程审核（CourseReview）、附件审核（AttachmentReviewPage）、课程列表管理（AdminCourseList）完成；学员端公开课程中心（CourseCatalog）、公开课程详情页（CourseDetailPublic）完成；全文档同步更新完成状态 |
| 2026-05-14 | v2.9 | **v2.9 完成确认** — 超管后台完善：AdminModule 统计接口 + 用户管理（搜索/筛选/启用禁用/删除）+ 教师管理（分页/详情弹窗/认证/取消认证）+ AdminDashboard 真实数据绑定 + 创建审核员/运营员账号弹窗；自动化测试 28 个用例全部通过 |
| 2026-05-14 | v2.9 | **推荐键与热门课程联动** — 超管在课程列表设置推荐后，首页热门课程区域自动优先展示"已推荐"课程；后端 findPublished() 排序增加 isRecommended DESC 优先展示推荐课程；前端 HomePage.vue 热门课程按 isRecommended + 创建时间排序 |
| 2026-05-14 | v3.1 | **v3.1 开始** — 订单模块 + 购物车开发启动 |
| 2026-05-15 | v3.1 | **个人中心功能完成** — 三端（制作人/教师/管理）统一 ProfilePage 组件，支持头像更换、基本信息编辑、账号安全概览 |
| 2026-05-15 | v3.1 | **教师设置迁移完成** — 教师端侧边栏移除"教师设置"改为个人中心内条件渲染；后端新增 PUT /users/profile 手机号正则校验、PUT /teachers/settings 修复 404 |
| 2026-05-15 | v3.1 | **通知偏好独立保存** — 教师端通知偏好 toggle 开关改为独立即时保存，不再依赖收款账号的保存按钮（ProfilePage.vue） |
| 2026-05-15 | v3.1 | **超管订单管理统计数据修复** — 后端新增 getOrderStats() 统计查询；controller 改用 success() 包装以保留 stats（原 successWithPagination() 丢弃额外字段） |
| 2026-05-15 | v3.1 | **页面跳转 404 修复** — 结算页"返回修改"、课程详情页"立即购买"跳转路径修复（/cart→/producer/cart, /producer/checkout→/checkout）；试看弹窗关闭按钮与文字重叠修复 |
| 2026-05-15 | v3.2 | **v3.2 支付宝支付对接完成** — 后端 PaymentsModule（支付宝配置/alipay-sdk 集成/异步通知验签/退款/订单状态更新）；前端 payment.ts + PaymentSuccess.vue + Checkout/MyOrders/OrderManage 三页联调；路由新增 /payment/success 支付成功页 |
| 2026-05-15 | v3.2 | **v3.2 自动化测试 18/18 全部通过** — 修复 createPayment() 中校验顺序（先业务后技术）；补充 .env 支付宝沙箱密钥配置；后端启动日志显示"支付宝 SDK 初始化成功，网关: sandbox"；生成真实支付链接 URL |
| 2026-05-15 | v3.2 | **支付宝沙箱密钥修复完成** — 使用 `支付参考/alipay.js` 中的正确 AppId（2021000149600403）更新 `.env`；修复 `confirmPayment` 的支付宝查询响应结构（`alipay_trade_query_response` 字段）；添加 `cross-env NODE_OPTIONS=--openssl-legacy-provider` 解决 OpenSSL 3.x 兼容问题；优化 PaymentSuccess.vue 页面异常处理逻辑 |
| 2026-05-15 | v3.3 | **系列课单课单独购买功能完成** — Lesson 实体新增 `canSinglePurchase`（是否支持单独购买）和 `singlePrice`（单独购买价格）字段；新建 `UserLesson` 实体记录用户已购课时；`OrderItem` 新增 `lessonId` 字段支持课时级订单；订单常量新增 `OrderType.SINGLE_LESSON`；课时创建/更新 DTO 支持单独购买价格设置；`courses.service.ts` 查课时增加播放权限检测（已购课时正常播放、已购系列课全量播放、免费课时播放、试看时长限制）；`orders.service.ts` 重写支持课时级订单创建；前端 `LessonInfo` 类型增加 `canSinglePurchase`/`singlePrice` 字段；`CourseDetailPublic.vue` 课时列表展示"单独购买"按钮；`Checkout.vue` 支持课时单独购买确认与下单；提供 `set-lesson-prices.sql` 预设价格脚本 |
| 2026-05-15 | v3.3 | **优惠券模块完成** — 后端 CouponsModule（Coupon/UserCoupon Entity、DTO、Service、Controller）+ 前端 CouponManage.vue + API 层 coupon.ts + 路由 + 侧边栏导航；支持满减券/折扣券、创建/编辑/启禁用/删除、分页搜索筛选 |
| 2026-05-15 | v3.3 | **v3.3 自动化测试 21/21 全部通过** — 修复测试脚本 `global` 声明缺失导致 token 无法共享的问题；管理端 CRUD 8 个 + 用户端领取 5 个 + 优惠券计算 4 个 + 权限安全 4 个，全部通过 |
| 2026-05-15 | **v3.4** | **收益模块补全完成** — 后端创建 EarningsModule（Earning Entity + EarningsService + EarningsController）；订单支付成功时事务创建收益记录（分教师收益/平台分成）；订单退款时事务扣减收益；教师端收益中心页（TeacherEarnings.vue）已存在并接入后端 API；管理端新增收益看板（AdminEarnings.vue）显示平台总览统计+每日趋势+课程/教师收益排行榜；新增 earnings.ts API 层统一管理端/教师端接口；路由与侧边栏导航同步更新 |
| 2026-05-15 | **代码清理** | **后端测试代码清理** — 删除 `backend/src/app.controller.spec.ts`（Jest 单元测试）、`backend/test/` 目录（e2e 测试）、`backend/scripts/` 根目录下的 6 个冗余脚本（与 `scripts/seeds/` 重复的 create-accounts/seed/seed_courses/seed-earnings-demo + 测试脚本 test-alipay-query/diagnose-alipay）；`backend/package.json` 移除 Jest 相关依赖和测试脚本命令。所有测试代码统一归入 `测试脚本与问题记录/` 目录下的 Python 文件 |
| 2026-05-16 | **CoursePlayer 导航优化** | **课程播放页导航优化** — "返回"按钮改为跳转到课程详情页（`/courses/:id`），替代原有 `router.back()` 回退行为；右上角新增"我的课程"按钮（带书本图标），点击跳转到制作人端我的课程列表（`/producer/my-courses`） |
| 2026-05-16 | **v4.1** | **视频播放器组件 + 学习进度追踪模块完成** — 新建 VideoPlayer.vue 专用播放器（速度/画中画/全屏/键盘快捷键）；后端新建 LearningModule（4 个 API 端点）；学习进度自动同步（每 30 秒 + 暂停时）；断点续播、完成标记；课件下载；侧边栏完成状态标记 |
| 2026-05-16 | - | **修复学习进度 API 三个 Bug** — duration 类型校验 `@IsInt`→`@IsNumber`；`@CurrentUser('userId')`→`@CurrentUser('sub')`；并发 `ER_DUP_ENTRY` 冲突兜底处理 |
| 2026-05-16 | - | **ProducerMyCourses 进度对接后端 API** — courseProgress() 从固定返回 0 改为从后端 learning API 读取实际进度数据 |
| 2026-05-16 | - | **修复教师端收益明细金额少 100 倍** — 后端 earnings.controller 已转换分→元，前端模板重复 /100 导致 |
| 2026-05-16 | - | **修复章节编排顺序错乱** — chapters.service.ts create() 新增自动计算最大 sortOrder 并 +1 |
| 2026-05-16 | - | **修复封面上传后返回 MinIO 对象名** — uploadCover() 返回前调用 getPresignedUrl() 转为可访问 URL |
| 2026-05-16 | - | **修复审核端待审核课程封面不显示** — findPendingReview() 遗漏 transformCoverToUrl() 调用 |
| 2026-05-16 | - | **增强超管控制台统计** — AdminService.getStats() 新增 5 个字段；AdminDashboard 统计卡片 4→8 个 |
| 2026-05-16 | - | **新增审核员工作量统计功能** — 基于 course_reviews 表统计；超管看全部，审核员只看自己；课程审核/附件审核页均展示 |
| 2026-05-16 | - | **审核员可预览未审核课程** — 审核详情弹窗新增"预览课程"按钮；新增 `GET /courses/admin/preview/:id` 接口供审核员/管理员预览任意状态课程 |
| 2026-05-16 | - | **课程详情弹窗全面增强** — 新增封面预览、正价/原价显示、标签/申请类型展示、系列课章节/课时树形目录预览（含视频有无标记）；弹窗宽度增至 640px |
| 2026-05-16 | - | **新增课程变更对比功能** — Course 实体新增 `previousData` 字段存储旧版本快照；`submitForReview()` 自动保存旧值；新增 `GET /courses/:id/diff` 接口；审核详情弹窗展示字段级对比（课程名称/价格/分类/简介等）允许审核员查看教师修改了哪些内容 |
| 2026-05-16 | - | **变更对比支持视频变更检测** — 快照新增 `trailerVideoId` 字段；`getCourseDiff()` 联查 lessons 表检测课时视频变更 |
| 2026-05-16 | - | **申请类型区分** — 新增 `getApplyTypeLabel()`/`getApplyTypeClass()` 方法，正确区分上架申请（蓝）/修改申请（黄）/下架申请（橙）三种类型 |
| 2026-05-16 | - | **审核员控制台数字徽章对齐修复** — `AdminDashboard.vue` 快捷操作面板中待审核数字徽章改为左对齐 |
